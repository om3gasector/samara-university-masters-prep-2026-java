| Задача                                    | Время      | Память | Примечание                              |
| ----------------------------------------- | ---------- | ------ | --------------------------------------- |
| 1. Самая короткая последовательность      | O(n)       | O(1)   | Один проход по массиву                  |
| 2. Среднее слева от диагоналей            | O(n²)      | O(1)   | Один проход по матрице                  |
| 3. Среднее справа от диагоналей (включая) | O(n²)      | O(1)   | Один проход по матрице                  |
| 4. Среднее справа от диагоналей (строго)  | O(n²)      | O(1)   | Один проход по матрице                  |
| 5. Самая длинная последовательность       | O(n)       | O(1)   | Один проход по массиву                  |
| 6. Среднее под диагоналями                | O(n²)      | O(1)   | Один проход по матрице                  |
| 7. Среднее слева от диагоналей (включая)  | O(n²)      | O(1)   | Один проход по матрице                  |
| 8. Медиана массива                        | O(n²)      | O(n)   | Пузырьковая сортировка, копия массива   |
| 9. Среднее под диагоналями (включая)      | O(n²)      | O(1)   | Один проход по матрице                  |
| 10. Среднее над диагоналями (включая)     | O(n²)      | O(1)   | Один проход по матрице                  |
| 11. Среднее над диагоналями (строго)      | O(n²)      | O(1)   | Один проход по матрице                  |
| 12. Ближайший общий родитель              | O(h)       | O(h)   | h - высота дерева, HashSet предков      |
| 13. Развёртывание по спирали              | O(n²)      | O(n²)  | Каждый элемент ровно один раз           |
| 14. Неубывающая подпоследовательность     | O(n²)      | O(n)   | ДП, массивы dp и prev                   |
| 15. Разбиение с мин. разностью            | O(n × sum) | O(sum) | Задача о рюкзаке, sum - сумма элементов |
